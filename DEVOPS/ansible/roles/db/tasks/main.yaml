- name: Pull MongoDB image
  docker_container:
    name: mongodb
    image: mongo:6
    state: started
    restart_policy: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db